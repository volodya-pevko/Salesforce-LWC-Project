import { LightningElement } from 'lwc';

export default class ButtonSendFormattedEmail extends LightningElement {
    //Button click sends formatted email to Lead email address
    handleClick() {
        
    }
}